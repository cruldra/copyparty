# CopyParty 项目目录结构

## 项目概述

CopyParty 是一个便携式文件服务器，支持加速断点续传上传、去重、WebDAV、FTP、零配置、媒体索引、视频缩略图、音频转码和只写文件夹等功能。

## 根目录文件

```
copyparty/
├── CODE_OF_CONDUCT.md      # 行为准则
├── CONTRIBUTING.md         # 贡献指南
├── LICENSE                 # MIT 许可证
├── README.md              # 项目说明文档（英文）
├── README_ZH.md           # 项目说明文档（中文）
├── SECURITY.md            # 安全政策
├── pyproject.toml         # Python 项目配置文件
├── setup.py               # Python 安装脚本
├── flake.nix              # Nix 包管理配置
└── flake.lock             # Nix 锁定文件
```

## 核心目录结构

### 1. copyparty/ - 主要源代码目录

```
copyparty/
├── __init__.py            # 包初始化文件
├── __main__.py            # 主入口点
├── __version__.py         # 版本信息
├── authsrv.py            # 认证服务
├── broker_*.py           # 代理相关模块（多进程/线程）
├── cert.py               # 证书管理
├── cfg.py                # 配置管理
├── dxml.py               # XML 处理
├── fsutil.py             # 文件系统工具
├── ftpd.py               # FTP 服务器
├── httpcli.py            # HTTP 客户端
├── httpconn.py           # HTTP 连接
├── httpsrv.py            # HTTP 服务器
├── ico.py                # 图标处理
├── mdns.py               # mDNS 服务发现
├── metrics.py            # 指标收集
├── mtag.py               # 元数据标签
├── multicast.py          # 多播支持
├── pwhash.py             # 密码哈希
├── smbd.py               # SMB 服务器
├── ssdp.py               # SSDP 协议
├── star.py               # 星标功能
├── sutil.py              # 字符串工具
├── svchub.py             # 服务中心
├── szip.py               # 压缩功能
├── tcpsrv.py             # TCP 服务器
├── tftpd.py              # TFTP 服务器
├── th_*.py               # 缩略图相关（客户端/服务端）
├── u2idx.py              # 上传索引
├── up2k.py               # 上传功能
├── util.py               # 通用工具
├── bos/                  # BOS 相关模块
│   ├── __init__.py
│   ├── bos.py
│   └── path.py
├── res/                  # 资源文件
│   ├── __init__.py
│   └── insecure.pem      # 不安全证书（开发用）
├── stolen/               # 第三方代码
│   ├── __init__.py
│   ├── dnslib/           # DNS 库
│   ├── ifaddr/           # 网络接口地址
│   ├── qrcodegen.py      # 二维码生成
│   └── surrogateescape.py # 编码处理
└── web/                  # Web 前端资源
    ├── Makefile          # 构建脚本
    ├── *.html            # HTML 页面
    ├── *.css             # 样式文件
    ├── *.js              # JavaScript 文件
    ├── *.gif             # 图像资源
    ├── a/                # 应用相关
    └── deps/             # 依赖文件
```

### 2. bin/ - 可执行脚本和工具

```
bin/
├── README.md             # 工具说明
├── *.py                  # Python 工具脚本
├── *.sh                  # Shell 脚本
├── handlers/             # 请求处理器
│   ├── README.md
│   └── *.py             # 各种处理器脚本
├── hooks/                # 钩子脚本
│   ├── README.md
│   └── *.py/*.js        # 事件钩子脚本
└── mtag/                 # 元数据标签工具
    ├── README.md
    ├── *.py             # 标签处理脚本
    ├── install-deps.sh  # 依赖安装
    └── res/             # 资源文件
```

### 3. contrib/ - 贡献和集成

```
contrib/
├── README.md             # 贡献说明
├── apache/               # Apache 配置
├── haproxy/              # HAProxy 配置
├── lighttpd/             # Lighttpd 配置
├── nginx/                # Nginx 配置
├── traefik/              # Traefik 配置
├── systemd/              # Systemd 服务配置
├── openrc/               # OpenRC 服务配置
├── nixos/                # NixOS 模块
├── package/              # 包管理配置
│   ├── arch/            # Arch Linux
│   ├── makedeb-mpr/     # Debian
│   ├── nix/             # Nix
│   └── rpm/             # RPM
├── plugins/              # 插件
├── themes/               # 主题
├── ios/                  # iOS 快捷方式
├── windows/              # Windows 相关
└── *.sh/*.bat/*.reg      # 各种配置脚本
```

### 4. docs/ - 文档

```
docs/
├── README.md             # 文档索引
├── changelog.md          # 更新日志
├── devnotes.md           # 开发笔记
├── design.txt            # 设计文档
├── versus.md             # 对比文档
├── example*.conf         # 配置示例
├── *.md                  # 各种说明文档
├── *.html                # HTML 文档
├── *.svg                 # Logo 文件
├── copyparty.d/          # 配置目录示例
├── cursed-usecases/      # 特殊用例
├── examples/             # 示例
│   ├── README.md
│   ├── docker/          # Docker 示例
│   └── windows.md       # Windows 示例
└── rice/                 # 定制化
    ├── README.md
    └── rtl.patch        # RTL 补丁
```

### 5. scripts/ - 构建和开发脚本

```
scripts/
├── *.sh/*.py             # 各种构建脚本
├── bench/                # 性能测试
├── deps-docker/          # Docker 依赖
├── docker/               # Docker 构建
│   ├── Dockerfile.*     # 各种 Dockerfile
│   ├── Makefile
│   └── README.md
├── lics/                 # 许可证相关
├── patches/              # 补丁文件
├── py2/                  # Python 2 兼容
├── pyinstaller/          # PyInstaller 打包
├── strip_hints/          # 类型提示移除
└── test/                 # 测试脚本
```

### 6. tests/ - 测试代码

```
tests/
├── __init__.py           # 测试包初始化
├── run.py                # 测试运行器
├── util.py               # 测试工具
├── test_*.py             # 各种测试模块
├── ptrav.py              # 路径遍历测试
└── res/                  # 测试资源
    └── idp/             # IDP 测试资源
```

### 7. srv/ - 服务示例

```
srv/
├── *.html                # HTML 示例页面
├── *.md                  # Markdown 示例
└── expand/               # 扩展示例
```

## 主要功能模块

1. **HTTP/HTTPS 服务器** - `httpsrv.py`, `httpconn.py`
2. **文件上传/下载** - `up2k.py`, `httpcli.py`
3. **多协议支持** - `ftpd.py`, `tftpd.py`, `smbd.py`
4. **认证和安全** - `authsrv.py`, `cert.py`, `pwhash.py`
5. **媒体处理** - `th_*.py`, `mtag.py`, `ico.py`
6. **网络发现** - `mdns.py`, `ssdp.py`, `multicast.py`
7. **配置管理** - `cfg.py`
8. **Web 界面** - `web/` 目录下的前端资源

## 构建和部署

- **开发环境**: 使用 `scripts/` 下的开发脚本
- **打包**: 支持 PyInstaller、Docker、各种包管理器
- **配置**: 参考 `docs/example*.conf` 和 `contrib/` 下的配置示例
- **测试**: 使用 `tests/` 目录下的测试套件

这个项目结构清晰地分离了核心功能、工具脚本、文档、测试和部署配置，便于开发和维护。
